namespace Lumyte.Graphics.TwoD;

/// <summary>Controls how a gradient color line is sampled outside its first and last stops.</summary>
public enum GradientExtendMode
{
    Pad,
    Repeat,
    Reflect,
}
